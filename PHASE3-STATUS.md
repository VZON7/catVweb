# 阶段③ 云端同步 — 进度存档

> 分支 `phase3-sync`，**未合并、未上线**。线上仍是第 223 次。
> **2026-09-16：验证通过，阻塞解除，可以走上线流程了。**

---

## 一、当前状态

代码写完，34 项回归测试全过，真实 Supabase 环境端到端验证通过。
原先唯一挡着上线的现象（**云端数据被回退成旧版本**），
在「单窗口 + 版本 14」的条件下**没有复现** —— 「旧标签页」假设成立。

### 验证记录（2026-09-16，代码版本 14，只开一个窗口）

| # | 账号 | 云端拉到 | 本机 | 服务器核对 | |
|---|---|---|---|---|---|
| 1 | theprov7 | 9 | 9 | 9 | |
| 2 | vzonexcel | 5 | 5 | 5 | 有变化 |
| 3 | vzonexcel | 5 | 5 | 5 | |
| 4 | vzonexcel | 5 | **6** | **6** | 有变化 ← 新记 1 条 |
| 5 | vzonexcel | **6** | 6 | 6 | |
| 6 | theprov7 | **9** | 9 | 9 | 有变化 |
| 7 | vzonexcel | **6** | 6 | 6 | 有变化 |
| 8 | vzonexcel | 6 | 6 | 6 | |

**判读：**
- 第 4→5 行：新记的那条上传后再拉回来仍在（5→6→6），**没退回**
- 第 1→6 行：theprov7 全程 9 条，期间 vzonexcel 反复写入，**一条没被波及**
- 第 6→7 行：切走再切回，vzonexcel 仍是 6 条 —— **上次正是在这里崩的**
- 数字**只升不降**；两个账号数据各归各的，无串账

**边界（诚实记一笔）：** 一次干净运行证明的是「这次没犯」，不是「永远不会犯」。
没复现的证据，天然比复现出来的弱。但判读标准是事先定好的，
证伪条件（N 偏小）没有出现，就按约定认它通过。

### ❗️ 已被排除的线索（别再往这个方向查）

版本 14 日志里的 `token?grant_type=password 400` —— 经用户确认只是**输错邮箱**，
属于正常的登录拒绝。同批日志里的「云端 4 条 + 本机新增 4 条 = 8 条」
在 merge 模式下也是正常结果，不构成串账证据。

**教训：不要从单张日志截图推断根因。**
`400` 只说明请求被拒，不说明为什么被拒。

---

## 二、第 224 次 update 修了什么（2026-09-16）

三处，都在 `journal.html`：

**1. 登录 / 注册失败时清空身份**

`cloudSignIn` / `cloudSignUp` 失败时没清 `cloudUser`，
程序会继续拿上一个账号的身份同步，`cj_owner` 从此跟真实账号对不上。
现在失败一律 `cloudUser=null` + `setSync('off')`；
注册后「等邮箱点确认链接」也算没真登上，同样清空。

**2. 同步前身份复核**

`cloudSync` 开头用 `auth.getSession()` 核对真实 session 跟 `cloudUser.id`
是否一致。对不上就清掉身份并报错停手 —— 宁可这次不同步，
也绝不把数据写进别人的格子。

**3. 补 `mobile-web-app-capable`**

控制台那条弃用黄字，纯整洁，不影响功能。

`APP_BUILD` 和 `sw.js` 的 `VERSION` 已同步提到 **15**。34 项回归测试重跑全过。

> 这三处**跟上面那个回退现象没有因果关系**，属于顺手补的健壮性漏洞。

---

## 二之二、第 225 次 update 修了什么（2026-09-16）

都在同步弹窗，前两项是实际用下来发现的。

**1. 「立即同步」点完自动关窗**

它用的是主按钮样式，长得像「确定」，点了窗口不动会让人以为没生效。
现在成功就关窗，出错才留着（错误信息就显示在窗口里）。
侧栏「同步」那颗圆点本来就在显示状态，关了窗照样看得见。

**2. ⚠️ 「一并清除」前先确认真的推成功了**

**这是个能真丢数据的路径。** 以前 `doSignOut(true)` 是无条件清本机的：
`cloudSignOut` 里的补推调用 `cloudSync()`，而 `cloudSync` 自己把错误 catch 掉、
不往外抛，所以外面那层 `try/catch` 永远捕不到东西 —— 云端一挂（比如项目被自动
暂停、502），补推静默失败，本机照删，没传上去的记录就真没了，
界面还写着「登录回来即可恢复」。

现在 `cloudSignOut` 返回 `pushedOk`（跑完看 `_dirty` 和 `syncState` 判断），
没推成功就**拒绝清除**，退化成只登出，并明确告诉用户数据还在这台设备上。

**3. 「记住我」勾选框 —— 只记邮箱，不存密码**

密码交给浏览器自带的密码管理器：把输入框的 `autocomplete="off"` 放开，
改成 `username` / `current-password`（注册时 `new-password`）。
那个 `off` 正是一直把浏览器密码管理器挡在门外的原因。

**为什么不自己存密码：** 明文进 localStorage，同一台电脑上任何人按 F12 就能看到，
跟「一并清除」保护共用设备的初衷直接打架。浏览器那套有系统级加密，交给它。
「一并清除」时连记住的邮箱一起抹掉（`cj_sync_remember` / `cj_sync_email`），
因为邮箱也是痕迹。

**4. 邮箱栏按 Enter 跳到密码栏**

密码栏按 Enter 仍是直接登录，没变。

`APP_BUILD` 和 `sw.js` 的 `VERSION` 提到 **16**。34 项回归测试全过。

---

## 三、下次开工第一步

把验证序列**用版本 16 再跑一遍**，确认这三处改动没碰坏同步：

```
1. 关掉所有 localhost:8080 的标签页和无痕窗口，一个不留
2. 开一个新的，Console 确认第一行是 [catVweb] 代码版本 16
3. 跑：登录 A → 记 1 条 → 登出 → 登录 B → 登出 → 登录回 A
4. 看最后一行日志的「云端拉到 N 条」—— 数字只升不降就对了
```

⚠️ **每一步之前都确认版本号是 16。** 改完代码浏览器多半还捧着旧的，
硬刷新（Ctrl+Shift+R），不行就 Application → Service Workers → Unregister。

### 排查工具

- 控制台日志：`[sync] 账号 · 云端拉到 X 条 → 本机 Y 条 → 服务器核对 Z 条 · merge/replace`
- 数据库对账 SQL 见 `catvweb-journal-SKILL.md` 第三十四节
- 本地起服务：仓库没有现成脚本，随便一个静态服务器指到仓库根目录、开 8080 即可

---

## 四、必须注意的坑（都踩过）

| 坑 | 表现 | 防范 |
|---|---|---|
| **Service Worker 送旧代码** | 拿旧代码测新修复，结论全错 | 每次先看控制台版本号；改完 `journal.html` 必须同时加 `APP_BUILD` 和 `sw.js` 的 `VERSION` |
| **Supabase 免费版会自动暂停** | 闲置约 7 天后服务停摆。登录报 `Failed to fetch`，控制台一片 `502 Bad Gateway` / `521`，还附带一堆 **CORS 红字** | **别去查 CORS，那是假象** —— 服务器吐错误页时不带 CORS 头，浏览器才这么报。去 Supabase 控制台点 Restore，等几分钟。命令行 `curl` 打 `/auth/v1/health` 能一秒分清是服务器还是代码 |
| **多标签页互相覆盖** | 云端数据莫名回退 | 测试时只留一个窗口（2026-09-16 已证实这就是当初那个现象的原因） |
| **静默失败** | 界面显示正常，数据没了 | 已加：上传后读回核对、锁在 finally 释放、上传安全闸 `wouldLoseRecords()` |
| **日志不写账号** | 多账号测试时分不清哪行是谁的 | 已修，日志带账号名 |

---

## 五、上线前的清单

- [x] ~~验证「旧标签页」假设~~ —— 2026-09-16 通过
- [x] ~~修登录失败不清身份的漏洞~~ —— 第 224 次 update
- [ ] 用版本 16 重跑一遍验证序列（见第三节）
- [ ] **导出一份备份**存到电脑外面（数据只有 7.5 KB，十秒钟）
- [ ] 删掉 Supabase 里的测试账号（Authentication → Users，`probe*` `e2e*` `sec*` `rpa*` `rpb*`）
- [ ] 决定账号策略：手动建号（推荐，10 个朋友点 10 次）还是开放注册
- [ ] 合并 `phase3-sync` 到 `main`
- [ ] 上线后先自己单独用 2–3 天，再叫朋友

---

## 六、暂缓的想法

**云端历史版本**：每次上传前把旧版本存进历史表，保留最近 20 份，
出问题自己点一下恢复。按 7.5 KB 的数据量，10 个人也才 1.5 MB，几乎免费。

**为什么暂缓：** 需要新表 + 改上传逻辑 + 做恢复界面，全是新的未测代码。
刚排查完一堆静默失败，不该在没扫干净的地上继续堆。等同步稳定跑几天再加。

替代品：手动导出备份（7.5 KB，贴哪都行），效果一样，只是要自己动手。

---

## 七、资源用量（按真实数据 7.5 KB 算）

| 场景 | 每月流量 | 免费额度占比 |
|---|---|---|
| 现在（29 条） | 79 MB | 1.5% |
| 涨 10 倍 | 475 MB | 9% |
| 涨 100 倍（约 3000 条） | 4.3 GB | 87% |

数据库占用：10 人涨 100 倍也才 7.3 MB / 500 MB。**流量在可见的未来不是问题。**
（开发中期我曾按 300 KB 估算并做了省流量优化，那个估算高估了 40 倍；
优化本身无害，减少请求也就减少了出错机会，留着。）
