# 阶段③ 云端同步 — 进度存档

> **2026-09-16 已上线。** 线上是第 231 次（`APP_BUILD` / `sw.js VERSION` 均为 22，含注册验证码）。
> 部署分支 `claude/amazing-bohr-zIAEL`，提交 `541a4f4`。
> 上线前的版本是 `f030c05`（第 223 次），回退命令见「部署真相」一节。

---

## 一、当前状态

代码写完，34 项回归测试全过，真实 Supabase 环境端到端验证通过。
原先唯一挡着上线的现象（**云端数据被回退成旧版本**），
在「单窗口 + 版本 14」的条件下**没有复现** —— 「旧标签页」假设成立。

### 验证记录（2026-09-16，代码版本 14，只开一个窗口）

| # | 账号 | 云端拉到 | 本机 | 服务器核对 | |
|---|---|---|---|---|---|
| 1 | theprov7 | 9 | 9 | 9 | |
| 2 | vzonexcel | 5 | 5 | 5 | 有变化 |
| 3 | vzonexcel | 5 | 5 | 5 | |
| 4 | vzonexcel | 5 | **6** | **6** | 有变化 ← 新记 1 条 |
| 5 | vzonexcel | **6** | 6 | 6 | |
| 6 | theprov7 | **9** | 9 | 9 | 有变化 |
| 7 | vzonexcel | **6** | 6 | 6 | 有变化 |
| 8 | vzonexcel | 6 | 6 | 6 | |

**判读：**
- 第 4→5 行：新记的那条上传后再拉回来仍在（5→6→6），**没退回**
- 第 1→6 行：theprov7 全程 9 条，期间 vzonexcel 反复写入，**一条没被波及**
- 第 6→7 行：切走再切回，vzonexcel 仍是 6 条 —— **上次正是在这里崩的**
- 数字**只升不降**；两个账号数据各归各的，无串账

**边界（诚实记一笔）：** 一次干净运行证明的是「这次没犯」，不是「永远不会犯」。
没复现的证据，天然比复现出来的弱。但判读标准是事先定好的，
证伪条件（N 偏小）没有出现，就按约定认它通过。

### ❗️ 已被排除的线索（别再往这个方向查）

版本 14 日志里的 `token?grant_type=password 400` —— 经用户确认只是**输错邮箱**，
属于正常的登录拒绝。同批日志里的「云端 4 条 + 本机新增 4 条 = 8 条」
在 merge 模式下也是正常结果，不构成串账证据。

**教训：不要从单张日志截图推断根因。**
`400` 只说明请求被拒，不说明为什么被拒。

---

## 二、第 224 次 update 修了什么（2026-09-16）

三处，都在 `journal.html`：

**1. 登录 / 注册失败时清空身份**

`cloudSignIn` / `cloudSignUp` 失败时没清 `cloudUser`，
程序会继续拿上一个账号的身份同步，`cj_owner` 从此跟真实账号对不上。
现在失败一律 `cloudUser=null` + `setSync('off')`；
注册后「等邮箱点确认链接」也算没真登上，同样清空。

**2. 同步前身份复核**

`cloudSync` 开头用 `auth.getSession()` 核对真实 session 跟 `cloudUser.id`
是否一致。对不上就清掉身份并报错停手 —— 宁可这次不同步，
也绝不把数据写进别人的格子。

**3. 补 `mobile-web-app-capable`**

控制台那条弃用黄字，纯整洁，不影响功能。

`APP_BUILD` 和 `sw.js` 的 `VERSION` 已同步提到 **15**。34 项回归测试重跑全过。

> 这三处**跟上面那个回退现象没有因果关系**，属于顺手补的健壮性漏洞。

---

## 二之二、第 225 次 update 修了什么（2026-09-16）

都在同步弹窗，前两项是实际用下来发现的。

**1. 「立即同步」点完自动关窗**

它用的是主按钮样式，长得像「确定」，点了窗口不动会让人以为没生效。
现在成功就关窗，出错才留着（错误信息就显示在窗口里）。
侧栏「同步」那颗圆点本来就在显示状态，关了窗照样看得见。

**2. ⚠️ 「一并清除」前先确认真的推成功了**

**这是个能真丢数据的路径。** 以前 `doSignOut(true)` 是无条件清本机的：
`cloudSignOut` 里的补推调用 `cloudSync()`，而 `cloudSync` 自己把错误 catch 掉、
不往外抛，所以外面那层 `try/catch` 永远捕不到东西 —— 云端一挂（比如项目被自动
暂停、502），补推静默失败，本机照删，没传上去的记录就真没了，
界面还写着「登录回来即可恢复」。

现在 `cloudSignOut` 返回 `pushedOk`（跑完看 `_dirty` 和 `syncState` 判断），
没推成功就**拒绝清除**，退化成只登出，并明确告诉用户数据还在这台设备上。

**3. 「记住我」勾选框 —— 只记邮箱，不存密码**

密码交给浏览器自带的密码管理器：把输入框的 `autocomplete="off"` 放开，
改成 `username` / `current-password`（注册时 `new-password`）。
那个 `off` 正是一直把浏览器密码管理器挡在门外的原因。

**为什么不自己存密码：** 明文进 localStorage，同一台电脑上任何人按 F12 就能看到，
跟「一并清除」保护共用设备的初衷直接打架。浏览器那套有系统级加密，交给它。
「一并清除」时连记住的邮箱一起抹掉（`cj_sync_remember` / `cj_sync_email`），
因为邮箱也是痕迹。

**4. 邮箱栏按 Enter 跳到密码栏**

密码栏按 Enter 仍是直接登录，没变。

`APP_BUILD` 和 `sw.js` 的 `VERSION` 提到 **16**。34 项回归测试全过。

---

## 二之三、第 226 次 update 修了什么（2026-09-16）

**1. 改密码**

已登录的同步弹窗里加了「改密码」入口：当前密码 + 新密码 + 再输一遍。

`cloudChangePw()` 先用**当前密码**调一次 `signInWithPassword` 验身份，
过了才调 `updateUser({password})`。
**为什么要多验一次：** Supabase 只要 session 有效就允许改密码，但共用电脑上
光凭「浏览器里还登着」就放行太松 —— 任何人坐下来都能把账号夺走。

**2. 修：取消勾选「记住我」，邮箱框不清空**

实际用下来发现的。`syncSetRemember(false)` 当时确实把 localStorage 的两个键
都删了，但没清内存里的 `_syncEmail`、也没重绘 —— 所以框里的邮箱还在，
看起来像没生效（刷新页面就会发现其实清了）。

现在：取消勾选会清空邮箱框并重绘；**但只清「从记忆里填进来的那个」** ——
如果你自己现打了一半，留着，别让人白打一遍。
另外勾上时立刻记下当前邮箱，不用等登录成功，否则「勾了却没记住」同样像坏了。

`APP_BUILD` 和 `sw.js` 的 `VERSION` 提到 **17**。34 项回归测试全过。

---

## 二之四、第 227 次 update 修了什么（2026-09-16）

全是文案和可见度，没动逻辑。

**1. 改密码入口从小链接改成整块按钮**

原来是 `sync-link` 样式的一行小字，太不明显。现在用 `bk-tog` 整块按钮
（标题 + 说明两行），跟登出弹窗那两个选项是同一套样式。

**2. 「登出」改成「同步并登出」**

这不是新行为，`cloudSignOut` 本来就会先把没传上去的改动推完再登出 ——
只是按钮上没写，用户看不出来。改个名字把既有的保障说出来。

**3. 登出二选一补上判断标准**

原来两个选项的说明只讲「会发生什么」，没讲「我该选哪个」。现在加一句总纲
「判断标准很简单：这台设备是不是你自己的」，选项标题也直接带上场景：
「保留在本机（自己的设备选这个）」「一并清除（借来的、公用的选这个）」。

风险提示也顺带更新了：第 225 次加了清除前的保险之后，
「没成功传上云端就不会清除」已经是事实，写出来让人安心。

`APP_BUILD` 和 `sw.js` 的 `VERSION` 提到 **18**。34 项回归测试全过。

---

## 二之五、第 228 次 update 修了什么（2026-09-16）

接着第 227 次的界面调整，两处都是用户看了实物之后提的。

**1. 同步弹窗的按钮层级**

第 227 次把「改密码」做成整块大按钮，结果跟主按钮抢戏，主次分不清；
而「同步并登出」和「立即同步」并排，两个都带「同步」两个字，也都像主按钮。

现在分三层：信息区 → 次要操作（「改密码」「登出」两个小按钮并排）
→ 底部唯一主按钮「立即同步」。按钮名改回「登出」，
「会先同步再登出」这个承诺挪进登出弹窗的副标题 —— 那才是做决定的地方。

**2. 登出二选一改成「场景当标题」**

原来标题是动作（「保留在本机」）、说明是场景，用户得在脑子里做一次转换。
现在直接把处境当标题，动作放第二行：

```
登出后，这台设备上的记录怎么处理？
登出前会先把没传上去的改动同步到云端。

  这是我自己的设备
  记录留在本机

  这台设备不是我的
  记录一并清除
```

每项从 30+ 字砍到 12 字。原来那句「判断标准很简单……」删掉了 ——
判断标准已经写进选项本身，不必再说一遍。

**保留在云端那份数据的安心话去哪了？** 挪到副标题「登出前会先把没传上去的
改动同步到云端」—— 位置更靠前，而且对两个选项都成立。

`APP_BUILD` 和 `sw.js` 的 `VERSION` 提到 **19**。34 项回归测试全过。

---

## 二之六、第 229 次 update 修了什么（2026-09-16）

**1. 按钮**

- 「改密码」再缩小一档（`padding:5px 9px; font-size:10px`）
- 「登出」搬回主按钮同一排，**靠左**（`margin-right:auto` 把它和主按钮推开），
  用新的 `.m-danger` 样式：**红字，不是红块**。登出默认并不删数据，
  红块过重会吓人；红字足够让人停一秒。

**2. 「一并清除」的话术**

原来第二行写「记录一并清除」，用户反馈**像是要删掉所有记录（包括云端）**。
改成「**只清这台设备，云端那份还在**」—— 前半句限定范围，后半句消除恐惧。

**3. 「这是我自己的设备」补上离线能力**

第二行改成「记录留在本机，**离线也能记录**」。

### ✅ 离线这条已核实（2026-09-16 读码确认）

- 离线时 `cloudSync()` 第一行就 `navigator.onLine===false` → 标成 offline 退出，
  但记录照常写进本机，`schedulePush()` 把 `_dirty` 置为 true
- 恢复联网时 `window.addEventListener('online')` 会自动调一次 `cloudSync()`，
  把攒下的改动推上去；也可以手动点「立即同步」
- **离线期间刷新或关掉浏览器也不会丢**：`_dirty` 和 `_lastStamp` 只在内存里，
  重开后都归零，于是 `stamp===_lastStamp` 必然不成立 → 不会走「云端无变化，跳过」
  那条捷径 → 强制走完整同步（拉 → 合 → 推），本机那些离线记录会被合并上传

前提是这台设备**登录过**（`cloudInit()` 从本机 session 恢复）。
从没登录过就一直是纯本机，等哪天登录时按「换账号保护」合并或覆盖。

`APP_BUILD` 和 `sw.js` 的 `VERSION` 提到 **20**。34 项回归测试全过。

---

## 二之七、第 230 次 update 修了什么（2026-09-16）

**登录 / 注册分不清**

用户反馈：点了「还没有账号？注册一个」之后，界面看起来跟登录一模一样，
不知道自己点中了没有 —— 直到点下主按钮蹦出「邮箱和密码都要填」，
才意识到原来已经在注册界面了。

原因：两个模式**只有底部一行小字和主按钮文案不一样**，
标题（「云端同步」）和说明文字一字未改，而眼睛是落在上方的。

改法（从三个方案里挑的，做了可点的预览页让用户实际体验后选定）：

```
┌ 云端同步 ─────────────────┐
│  ┌──────┬────────────┐   │
│  │ 登录 │ █注册新账号█ │   │  ← 分段切换，高亮当前那栏
│  └──────┴────────────┘   │
│ 填个邮箱和密码就行……        │  ← 说明两栏各写各的
│ 邮箱  [            ]      │
│ 密码  [            ]      │
│ ☐ 记住我                  │
│         [取消] [注册新账号] │
└──────────────────────────┘
```

**为什么选分段切换：** 另外两个方案（改标题、加确认密码栏）都要先点了那行
小字才发现有注册这回事。分段切换是唯一能让用户**在点之前就知道有两个模式**的，
而且高亮块滑动的反馈就发生在眼睛正看着的位置。

`syncSwitchMode()`（翻面）换成了 `syncSetMode(m)`（指定去哪一栏）——
再点一次同一栏应该什么都不发生，否则会把提示语清掉。

**顺带加了「忘记密码了？找 VZON 帮你重设。」**

只在**登录栏**出现（注册时还没有密码可忘）。
这句话把现状说清楚了：忘记密码目前确实只能找管理员，
处理办法见本文件「用户忘记密码时，管理员怎么帮他改」。
哪天真做了自助重设，这句再换掉。

`APP_BUILD` 和 `sw.js` 的 `VERSION` 提到 **21**。34 项回归测试全过。

---

## 二之八、还没做的（认证相关）

**忘记密码** —— 没做。Supabase 的 `resetPasswordForEmail` 流程有三个坑要先处理：

1. `cloudInit()` 现在看到网址里有 `access_token` 就清掉 hash。恢复链接回来时带的
   正是 `#access_token=...&type=recovery`，现在的行为会变成「静默把人登进去」，
   用户还是改不了密码。得先认出 `type=recovery` 再弹设置新密码的界面。
2. Supabase 后台 Authentication → URL Configuration 要把回跳网址加进白名单
   （localhost:8080 和正式网址都要）。
3. 免费版内置 SMTP 官方明说不适合正式使用，每小时只有几封，容易进垃圾箱。

### 用户忘记密码时，管理员怎么帮他改

**平时就用这个（一行搞定）：**

```bash
node tools/reset-password.js --setkey              # 存密钥，做一次就好
node tools/reset-password.js 他的邮箱 临时密码123    # 以后每次就这一行
node tools/reset-password.js --who 他的邮箱         # 只查 UUID，不改东西
```

在 VSCode 下面那个终端里跑，`cd` 到仓库目录即可。脚本会自己按邮箱查 UUID、
把英文错误翻成中文，并在密钥贴错（anon / publishable）时提前拦下来。

密钥存在 `tools/.admin-key`，**已写进 `.gitignore`，不会被提交**；
也可以改用环境变量 `CJ_ADMIN_KEY`，脚本优先读它。

改完把临时密码私下给他，让他登录后自己进「改密码」换掉。日记数据一条不动。

---

### 手动版（脚本坏了或者想弄明白它在干嘛时看这里）

**准备（只做一次）：拿密钥**

开 https://supabase.com/dashboard/project/buxqkndyfjhajdjwbcrp/settings/api-keys 
复制 `service_role`（旧式）或 `sb_secret_...`（新式），两者都行。

**第 1 步 — 找到那个人的 UUID**

开 https://supabase.com/dashboard/project/buxqkndyfjhajdjwbcrp/auth/users 
找到他的邮箱，点开，复制 UID（36 位带横杠那串）。

**第 2 步 — 想一个临时密码**

至少 6 位（我们的表单和 Supabase 都要求 6 位）。

**第 3 步 — 跑命令**

Git Bash：

```bash
curl -X PUT "https://buxqkndyfjhajdjwbcrp.supabase.co/auth/v1/admin/users/贴UUID" \n  -H "apikey: 贴密钥" \n  -H "Authorization: Bearer 贴密钥" \n  -H "Content-Type: application/json" \n  -d '{"password":"临时密码"}'
```

PowerShell：

```powershell
$key = "贴密钥"
$uid = "贴UUID"
$body = '{"password":"临时密码"}'
Invoke-RestMethod -Method Put -Uri "https://buxqkndyfjhajdjwbcrp.supabase.co/auth/v1/admin/users/$uid" -Headers @{ apikey = $key; Authorization = "Bearer $key" } -ContentType "application/json" -Body $body
```

方法必须是 **PUT** —— auth-js 的 `updateUserById` 就是 `PUT /admin/users/{uid}`。

**第 4 步 — 看返回**

成功会吐回那个用户的 JSON（有 `id`、`email`、`updated_at`）。常见失败：

| 返回 | 原因 |
|---|---|
| 401 Invalid API key | 密钥贴错，或者贴成 `anon` 了 |
| 403 User not allowed | 用的不是 service_role / secret 密钥 |
| 404 | UUID 错 |
| 422 Password should be at least 6 characters | 临时密码太短 |

**第 5 步 — 私下把临时密码给他**

别发公开群。让他登录后自己进「改密码」换成他要的。
改密码不影响他的日记数据。

⚠️ **service_role / secret 密钥能绕过数据围栏 (RLS)，等于所有人的全部数据。**
绝对不能写进 `journal.html`、不能提交进仓库。命令里带着密钥会留在 shell 历史里，
介意的话用完清一下。

后台那个「Send password recovery」按钮现在**帮不上忙** —— 用户点了邮件链接会被
静默登录进来，但「改密码」要求先输当前密码，他正是忘了才来的。
要让那条路走通，得先处理上面第 1 条（认出 `type=recovery`）。

**记多个邮箱** —— 没做。浏览器的密码管理器本来就能存多个账号，
点邮箱框会掉下列表。我们的「记住我」只记最后登录的那一个。
要做的话用 `<datalist>` 存最近几个即可。

---

## 二之九、❗️部署真相（2026-09-16 查实）

### 线上跑的是 `claude/amazing-bohr-zIAEL`，不是 `main`

网址：https://vzon7.github.io/catVweb/journal.html 
GitHub Pages 设置是「Deploy from a branch」，指的是那条分支。

**怎么查实的：** 把线上的 `journal.html` 抓下来算 SHA1，跟各分支逐一比对，
只有 `claude/amazing-bohr-zIAEL` 对得上；线上 `sw.js` 的 `VERSION = 3`、
有删除名单和新旧记号、没有 supabase —— 确实是第 223 次。

### ⚠️ `main` 是废的

`main` 停在 2026-06-17 的「update eng ver 1 (45)」，没有 `sw.js`、
没有 `manifest.json`、没有图标、没有删除名单。第 221 / 222 / 223 次
**一次都没合进去过**。

**所以：把 `phase3-sync` 合进 `main` 的话，线上一点变化都不会有。**
你会以为上线了，朋友那边还是第 223 次。

### 怎么会变成这样

```
2026-06-17  main 在「update eng ver 1 (45)」
            ├─ claude/vigilant-wozniak-mnsutx  做 UI/UX 优化
            │    → PR #10 合进 main → PR #11 又 revert 掉  ← 主干这条路走死
            └─ claude/amazing-bohr-zIAEL      ← 从此所有工作都在这条
                 2026-06-18 至今，64 个提交
```

那两个 `claude/xxx-yyy-zzz` 是 Claude 在 GitHub 上干活时自动开的
**一次性工作分支**，名字是随机生成的。main 那条路被 revert 之后，
真实工作就一直留在这条临时分支上，三个月后它事实上成了主干 —— 只是名字没改。

（本地用 Claude Code 跟这条分支无关，任何分支都能干活。）

### 迟早要理，但别跟上线一起做

这条分支本来是设计成用完即弃的，**任何人看到这名字都会想删它，删掉网站就下线**。

建议顺序：先在这条分支上上线 → 稳定几天 → 再单独把 `main` 重置成它的内容、
Pages 改指 `main`。两件事分开，出问题时每一步都只有一个变量。

### 回退怎么做

上线前的线上版本是 `f030c05`。真出事就把部署分支推回去：

```bash
git push origin f030c05:claude/amazing-bohr-zIAEL --force
```

Pages 会自动重新部署回第 223 次。**但云端已经产生的数据不会跟着回退** ——
所以上线前的备份才是真正的保险。

---

## 二之十、第 231 次 update：注册邮箱验证码（2026-09-16）

### 查到的现状

读 `/auth/v1/settings`（公开只读）：`mailer_autoconfirm: true`、`disable_signup: false` ——
**注册不发确认信，而且谁都能注册。** 网站公开上线后，这意味着任何人能拿别人的邮箱注册、
邮箱打错也能注册成功。用户决定：**保持开放注册，但注册必须输邮箱验证码。**

另外查实：`vaults` 外键 `confdeltype = c`（on delete cascade）—— 删账号时云端日记自动跟着删。
不登录读 `vaults` 返回 `[]`，数据围栏 (RLS) 开着。

### 代码做了什么

- `_syncMode` 多一个 `'code'`：注册拿不到 session 时停在「输验证码」
- `verifyOtp({email, token, type:'signup'})` 验证；`resend({type:'signup'})` 重发
- 「重新发送」按 Supabase 的规定锁 60 秒，服务器说要等 N 秒就按 N 秒锁；
  倒计时只改按钮文字，不重绘弹窗（否则正在输的验证码框会一直丢焦点）
- 注册完没输验证码就关窗的人，回来登录会撞到 `email_not_confirmed` → 自动带回验证码步
- **识破已注册邮箱**：开了验证后，Supabase 对已注册邮箱不报错，而是回一个
  `identities` 为空的假用户（防探测）。不识破的话界面会说「验证码已寄到」但其实没寄
- Supabase 英文报错翻中文（频率限制、验证码错/过期、已注册、账号密码错）

**后台开关没开时，注册照旧直接登录，根本走不到验证码那一步** —— 所以这版可以先上线。

已验证：用假 Supabase 跑 9 个场景 30 项断言全过（从 journal.html 原样抽函数测）；
34 项回归测试全过；16 个新文案键中英各一。

### ⚠️ 顺序不能反：先上代码，再开后台开关

**反过来会把人卡住** —— 旧版（21）不认识验证码，开了开关后注册的人只会看到
「确认信已寄出，去邮箱点链接」，而邮件里是数字，没地方输。

1. 推版本 22 上线，确认线上 `APP_BUILD=22`
2. **接自己的发信服务**（强烈建议，见下）
3. 改邮件模板：https://supabase.com/dashboard/project/buxqkndyfjhajdjwbcrp/auth/templates
   「Confirm sign up」那一个，正文换成下面这段（**不要留 `{{ .ConfirmationURL }}` 链接**，
   那个链接会跳到后台设的 Site URL，多半是打不开的 localhost）
4. 打开 Confirm email：https://supabase.com/dashboard/project/buxqkndyfjhajdjwbcrp/auth/providers
   里的 Email 那一项
5. 用一个**没注册过的邮箱**在线上实测一遍

**出问题的回退：把 Confirm email 关掉就行**，立刻恢复成注册直接登录，不用动代码。

邮件模板正文：

```html
<h2>你的 catVweb 验证码</h2>
<p>在注册窗口里输入这串数字：</p>
<p style="font-size:28px;font-weight:bold;letter-spacing:6px">{{ .Token }}</p>
<p>不是你本人在注册的话，忽略这封信就好。</p>
```

### ⚠️ 发信额度：内置服务每小时只有 2 封（整个项目合计）

Supabase 官方文档原文：内置发信服务 **2 emails per hour**，同一人两次发送至少隔 60 秒。
**这是全项目合计，不是每人 2 封** —— 叫 10 个朋友同一天注册，第 3 个起就收不到验证码了。

接自己的发信服务（SMTP）后，初始额度是每小时 30 封，还能在
https://supabase.com/dashboard/project/buxqkndyfjhajdjwbcrp/auth/rate-limits 里调高。
SMTP 设置页：https://supabase.com/dashboard/project/buxqkndyfjhajdjwbcrp/auth/smtp

没有自己域名的话，**Gmail + 应用专用密码**是最省事的选择（Resend 之类的服务
不验证域名只能发给自己，发不了给朋友）。

---

## 三、下次开工第一步

把验证序列**用版本 21 再跑一遍**，确认这三处改动没碰坏同步：

```
1. 关掉所有 localhost:8080 的标签页和无痕窗口，一个不留
2. 开一个新的，Console 确认第一行是 [catVweb] 代码版本 21
3. 跑：登录 A → 记 1 条 → 登出 → 登录 B → 登出 → 登录回 A
4. 看最后一行日志的「云端拉到 N 条」—— 数字只升不降就对了
```

⚠️ **每一步之前都确认版本号是 21。** 改完代码浏览器多半还捧着旧的，
硬刷新（Ctrl+Shift+R），不行就 Application → Service Workers → Unregister。

### 排查工具

- 控制台日志：`[sync] 账号 · 云端拉到 X 条 → 本机 Y 条 → 服务器核对 Z 条 · merge/replace`
- 数据库对账 SQL 见 `catvweb-journal-SKILL.md` 第三十四节
- 本地起服务：仓库没有现成脚本，随便一个静态服务器指到仓库根目录、开 8080 即可

---

## 四、必须注意的坑（都踩过）

| 坑 | 表现 | 防范 |
|---|---|---|
| **Service Worker 送旧代码** | 拿旧代码测新修复，结论全错 | 每次先看控制台版本号；改完 `journal.html` 必须同时加 `APP_BUILD` 和 `sw.js` 的 `VERSION` |
| **Supabase 免费版会自动暂停** | 闲置约 7 天后服务停摆。登录报 `Failed to fetch`，控制台一片 `502 Bad Gateway` / `521`，还附带一堆 **CORS 红字** | **别去查 CORS，那是假象** —— 服务器吐错误页时不带 CORS 头，浏览器才这么报。去 Supabase 控制台点 Restore，等几分钟。命令行 `curl` 打 `/auth/v1/health` 能一秒分清是服务器还是代码 |
| **多标签页互相覆盖** | 云端数据莫名回退 | 测试时只留一个窗口（2026-09-16 已证实这就是当初那个现象的原因） |
| **静默失败** | 界面显示正常，数据没了 | 已加：上传后读回核对、锁在 finally 释放、上传安全闸 `wouldLoseRecords()` |
| **日志不写账号** | 多账号测试时分不清哪行是谁的 | 已修，日志带账号名 |

---

## 五、上线前的清单

- [x] ~~验证「旧标签页」假设~~ —— 2026-09-16 通过
- [x] ~~修登录失败不清身份的漏洞~~ —— 第 224 次 update
- [x] ~~用版本 21 重跑验证序列~~ —— 2026-09-16 用户实测通过
- [x] ~~合并到 `claude/amazing-bohr-zIAEL` 并 push~~ —— 2026-09-16 完成，快进合并 16 个提交
- [x] ~~确认线上真的换了~~ —— 抓线上文件复查：`VERSION 21`、`APP_BUILD 21`，云端同步 /
      安全闸 / 身份复核 / 清除保险 / 记住我 / 改密码 / 分段切换 / 忘记密码 全部在位
- [ ] **导出一份备份**存到电脑外面（数据只有 7.5 KB，十秒钟）
- [ ] 删掉 Supabase 里的测试账号（Authentication → Users，`probe*` `e2e*` `sec*` `rpa*` `rpb*`）
- [ ] 决定账号策略：手动建号（推荐，10 个朋友点 10 次）还是开放注册
- [ ] 先自己单独用 2–3 天，再叫朋友
- [ ] 稳定之后再单独理分支：`main` 重置成部署分支的内容、Pages 改指 `main`

---

## 六、暂缓的想法

**云端历史版本**：每次上传前把旧版本存进历史表，保留最近 20 份，
出问题自己点一下恢复。按 7.5 KB 的数据量，10 个人也才 1.5 MB，几乎免费。

**为什么暂缓：** 需要新表 + 改上传逻辑 + 做恢复界面，全是新的未测代码。
刚排查完一堆静默失败，不该在没扫干净的地上继续堆。等同步稳定跑几天再加。

替代品：手动导出备份（7.5 KB，贴哪都行），效果一样，只是要自己动手。

---

## 七、资源用量（按真实数据 7.5 KB 算）

| 场景 | 每月流量 | 免费额度占比 |
|---|---|---|
| 现在（29 条） | 79 MB | 1.5% |
| 涨 10 倍 | 475 MB | 9% |
| 涨 100 倍（约 3000 条） | 4.3 GB | 87% |

数据库占用：10 人涨 100 倍也才 7.3 MB / 500 MB。**流量在可见的未来不是问题。**
（开发中期我曾按 300 KB 估算并做了省流量优化，那个估算高估了 40 倍；
优化本身无害，减少请求也就减少了出错机会，留着。）
